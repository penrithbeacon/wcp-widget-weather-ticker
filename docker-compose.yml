services:
  weather-ticker:
    build: .
    image: wcp-widget-weather-ticker
    container_name: wcp-widget-weather-ticker
    ports:
      - "3739:3739"
    restart: unless-stopped
    volumes:
      - weather_config:/app/data
      - ./published:/app/published

volumes:
  weather_config:
